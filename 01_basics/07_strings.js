const name = "Jaya "
const reposcount = 50


// console.log(name + reposcount + " value");
console.log(`Hello my name is ${name} and my repo is ${reposcount}`);

const gameName = new String("JayaThakur")
//by using this method we can get value directly in key value pair and can be accessed
console.log(gameName);
console.log(gameName[0]);
console.log(gameName.__proto__);
//(.) is used to use other methods with string

